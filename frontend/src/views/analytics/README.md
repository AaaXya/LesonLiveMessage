# 数据分析模块

后端算法目录：`src/analysis/`（见该包 `__init__.py` 的规划结构）

前端可视化目录（待创建，按需添加组件）：

```
frontend/src/views/analytics/
├── FrequencyChart.vue   # 弹幕频率趋势图
├── UserRanking.vue      # 用户活跃度排行
├── GiftStats.vue        # 礼物价值统计
└── KeywordCloud.vue     # 关键词云
```

图表库建议：ECharts（`vue-echarts`）或 PrimeVue 数据可视化组合。
