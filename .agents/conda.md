# 环境说明

当前项目使用 conda 环境进行 Python 开发和打包。

- 环境名：learnpy
- 运行 Python 时优先用：conda activate learnpy
- 相关命令：
    - python -m pip install ...
    - python -m PyInstaller ...
    - python scripts/build_exe.py

不要在 base 环境或其它非项目环境中执行打包命令。
